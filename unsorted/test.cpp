#include<bits/stdc++.h>
using namespace std;
int main(){
    int res=2;
    //cout<<min(res,10)<<"\n";
    //cout<<(1-(INT_MAX+1));
    set<vector<int>>s;
    unordered_set
    s.insert({1,2,3});
    s.insert({0,0,0});
    s.insert({0,0,0});
    for(auto i: s){
        for(auto j:i)
            cout<<j<<" ";
        cout<<"\n";
    }
    return 0;
}